# Vantablack theme — based on basecamp/omarchy
# https://github.com/basecamp/omarchy/tree/dev/themes/vantablack

$cursor_theme  = Bibata-Modern-Classic
$icon_theme    = Papirus-Dark

$background      = rgb(000000)
$foreground      = rgb(ffffff)
$accent          = rgb(8d8d8d)
$accent2         = rgb(cecece)
$inactive_border = rgb(404040)

$active_border_1 = rgb(8d8d8d)
$active_border_2 = rgb(cecece)

# Hyprlock semantic aliases
$base     = $background
$text     = $foreground
$surface0 = rgb(1a1a1a)
$red      = rgb(a4a4a4)
$yellow   = rgb(cecece)
$mauve    = $accent

# Alpha variants (for hyprlock placeholders)
$baseAlpha    = 000000
$textAlpha    = ffffff
$accentAlpha  = 8d8d8d
